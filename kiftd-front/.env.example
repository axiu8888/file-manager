# 须与后端 kiftd.api-prefix / 环境变量 KIFTD_API_PREFIX 一致（不含尾斜杠）
# 改成 /api2 时：后端 yml 改 api-prefix，这里改成 /api2，然后重启 Vite
VITE_API_PREFIX=/api

# 前端部署路径，须与 nginx location / Vite base 一致（含尾斜杠）
VITE_BASE=/kiftd/
