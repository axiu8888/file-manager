package com.kiftd.repository;

import com.kiftd.entity.FileChain;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FileChainRepository extends JpaRepository<FileChain, String> {
}
